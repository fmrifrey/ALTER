# seq, gnn, fbnetgen 
name: BrainNetworkTransformer
sizes: [360, 100]  # Note: The input node size should not be included here
pooling: [false, true]
readout: concat  # concat, mean, max, sum
pos_encoding: none  # rrwp, identity, none
orthogonal: true
freeze_center: true
project_assignment: true
pos_embed_dim: 32